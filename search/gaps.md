# search/gaps.md

Target role: Backend/Infrastructure Engineer, secondary interest in AI Infra / AI Engineer roles
SOC reference: 15-1252.00 (Software Developers)

| Gap | Evidence the target demands it | What I have | Plan to close it |
|---|---|---|---|
| **Kubernetes in production, not just listed as a skill** | Multiple AI infra postings (Scale AI Senior AI Infra Engineer, vCluster AI Infrastructure Engineer) list "experience with containers and orchestration tools (Docker, Kubernetes)" as baseline, and specify CKA-level depth or Kubernetes Operator experience as differentiators. | Kubernetes is listed in my skills section, but no bullet in resume.json describes a project where I deployed, scaled, or operated a Kubernetes workload in production. | Take one existing project (e.g., a Bedrock Agent service or the bulk provisioning system) and actually containerize + deploy it on EKS/GKE with autoscaling and monitoring, then document it as a shipped artifact (repo + README) I can point to in an interview. |
| **~~LLM serving / inference frameworks (vLLM, SGLang, TensorRT-LLM)~~ — KILLED** | ~~Scale AI's Senior AI Infrastructure Engineer, Model Serving Platform posting explicitly requires "experience with modern LLM serving frameworks such as vLLM, SGLang, TensorRT-LLM, or text-generation-inference."~~ | ~~My LLM project covers fine-tuning (LoRA, DPO) and training-side work, but nothing on production inference serving, batching, or KV-cache optimization.~~ | ~~Deploy one of my fine-tuned LLaMA models behind vLLM with continuous batching, benchmark throughput/latency, and publish the benchmark numbers + config as a public repo.~~ |
| **Infrastructure as Code (Terraform)** | Scale AI and vCluster postings both list Terraform/IaC as a required or strongly preferred skill for infra roles. | My IaC experience is limited to AWS CDK (from the Amazon Bedrock project) — I have no Terraform experience anywhere in my resume. | Rebuild the CDK-provisioned infra from a past project in Terraform instead, and push it as a public repo demonstrating equivalent IaC competence in the more commonly-required tool. |
| **GPU-based distributed training infrastructure** | AI Infra job postings and the AI-ML Infrastructure Engineering roadmap consistently list "distributed training frameworks (NCCL, DeepSpeed, FSDP)" and "multi-GPU parallelism" as core AI infra responsibilities. | My LLM fine-tuning project used LoRA on a single GPU (with CPU prototyping first) — no multi-GPU or distributed training framework experience. | Re-run a fine-tuning job using FSDP or DeepSpeed across multiple GPUs (rented cloud GPUs are sufficient), and document the setup and scaling results as evidence. |

## Notes on required edits (Step 3 requirements)

**Killed row:** "LLM serving / inference frameworks (vLLM, SGLang, TensorRT-LLM)" — killed because this gap targets AI infra roles focused on building/optimizing the serving framework and model runtime itself. My actual target within AI is building LLM-powered applications (agents, RAG systems, chatbots) on top of existing infra like Bedrock, not researching or operating the inference framework layer. This gap doesn't apply to the role I'm actually going for.

**Rewritten row:** Kubernetes gap, in my own words — I know Kubernetes exists and can talk about it conceptually, but I've never actually shipped anything on it. My Amazon Bedrock Agent project was serverless (Lambda-based), so it never touched container orchestration at all. What I have is theory, not a running system. My plan: take that same Bedrock Agent service and actually deploy it on Kubernetes (containerize the Action Group Lambdas equivalent as services, set up autoscaling), so I have a real, defensible "I built and deployed this on K8s" story instead of just "Kubernetes is listed in my skills."
