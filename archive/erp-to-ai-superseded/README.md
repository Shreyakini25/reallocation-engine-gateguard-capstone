# Superseded ERP-to-AI liveness artifacts

Replaced by `npm run ats:scan --dry-run` (Job-Ops layer).
Kept for provenance only — not used by the current mode.

- liveness-gate.mjs — hand-URL Playwright wrapper
- erp-to-ai-liveness-urls.txt — sample URLs for early run
