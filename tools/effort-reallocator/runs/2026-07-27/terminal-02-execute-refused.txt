==========================================================================
HARD STOP — committing application slots
==========================================================================
  proposal:    tools/effort-reallocator/out/proposal.json (generated 2026-07-27T18:07:36-04:00)
  gate status: BLOCKED
  moves:       8
    1 slot(s) ACME ANALYTICS LLC → MAPLEBEAR INC (stability 100.00%)
    1 slot(s) ACME ANALYTICS LLC → ROKU INC (stability 99.95%)
    1 slot(s) LINKEDIN CORP → HUMAN INC (stability 20.70%)
    1 slot(s) LINKEDIN CORP → PINTEREST INC (stability 70.70%)
    1 slot(s) ETSY INC → ROBLOX CORP (stability 70.25%)
    1 slot(s) AIRBNB INC → TELADOC HEALTH INC (stability 18.45%)
    1 slot(s) DOCUSIGN INC → TWILIO INC (stability 72.55%)
    1 slot(s) ZOOX INC → VISICON TECHNOLOGIES INC (stability 5.20%)

  FLAGS (carried, not blocking):
    [flag]  SKIP_RATE_BELOW_TARGET: 31.5% < 50.0%
            resolver: the human tightens the filter before spending the week

  BLOCKED — approval cannot clear these:
    [block] GATE_BLOCKING_UNWAIVED: DATASET_NO_RECORD_PROVENANCE
            why:      the data does not meet the stated quality standard, so no allocation computed from it is gradeable
            resolver: a named human waives each code with a written reason (--waive CODE --waiver-reason "..."), or the dataset is fixed
    [block] POSTING_NOT_VERIFIED: DOCUSIGN INC
            why:      liveness is a gate in the arithmetic and an assumption in this run; this firm's board cannot be read by the scanner at all
            resolver: a human opens the careers page and confirms a live, current posting for a matching title
    [block] POSTING_NOT_VERIFIED: HUMAN INC
            why:      liveness is a gate in the arithmetic and an assumption in this run; this firm's board cannot be read by the scanner at all
            resolver: a human opens the careers page and confirms a live, current posting for a matching title
    [block] POSTING_NOT_VERIFIED: PINTEREST INC
            why:      liveness is a gate in the arithmetic and an assumption in this run; this firm's board cannot be read by the scanner at all
            resolver: a human opens the careers page and confirms a live, current posting for a matching title
    [block] POSTING_NOT_VERIFIED: TELADOC HEALTH INC
            why:      liveness is a gate in the arithmetic and an assumption in this run; this firm's board cannot be read by the scanner at all
            resolver: a human opens the careers page and confirms a live, current posting for a matching title
    [block] POSTING_NOT_VERIFIED: ZOOX INC
            why:      liveness is a gate in the arithmetic and an assumption in this run; this firm's board cannot be read by the scanner at all
            resolver: a human opens the careers page and confirms a live, current posting for a matching title
    [block] POSTING_NOT_VERIFIED: VISICON TECHNOLOGIES INC
            why:      liveness is a gate in the arithmetic and an assumption in this run; this firm's board cannot be read by the scanner at all
            resolver: a human opens the careers page and confirms a live, current posting for a matching title
    [block] MOVE_NOT_STABLE: LINKEDIN CORP -> HUMAN INC (stability 20.70% < 70.00%)
            why:      the move is not distinguishable from leaving the allocation alone; spending a week on a coin flip is worse than spending it on habit
            resolver: the human decides whether to move anyway, on grounds the engine does not have — a referral, a conversation, a deadline
    [block] MOVE_NOT_STABLE: AIRBNB INC -> TELADOC HEALTH INC (stability 18.45% < 70.00%)
            why:      the move is not distinguishable from leaving the allocation alone; spending a week on a coin flip is worse than spending it on habit
            resolver: the human decides whether to move anyway, on grounds the engine does not have — a referral, a conversation, a deadline
    [block] MOVE_NOT_STABLE: ZOOX INC -> VISICON TECHNOLOGIES INC (stability 5.20% < 70.00%)
            why:      the move is not distinguishable from leaving the allocation alone; spending a week on a coin flip is worse than spending it on habit
            resolver: the human decides whether to move anyway, on grounds the engine does not have — a referral, a conversation, a deadline

  NOTHING MOVED. Decision logged: logs/gate-decisions/2026-07-27-effort-reallocator.md
  There is no --force flag. Resolve the blocks or accept the baseline.
